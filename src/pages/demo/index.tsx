import UploadPanel from '@/components/UploadPanel'

export default function index() {
  return (
    <div id={'use'} className={'w-full h-screen p-8 bg-blue-300'}>
      <UploadPanel />
    </div>
  )
}
